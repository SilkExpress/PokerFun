#include "stdafx.h"
#include "Odds.h"

using std::cout;
using std::endl;

/*
void Odds::findOdds(Deck& deck, Hand& hand)
{
	//hand.bestOption;
	
}

void Odds::findBest(std::vector<std::vector<Card*>>& options)
{
	for (int i = 0; i != options.size(); ++i) {
		//if (handValue(options[i]).value > bestValue.value) {
			//bestValue.value = handValue(options[i]).value;
			//bestValue.name = handValue(options[i]).name;
		//}
	}
}
*/

void findOdds(Deck& deck, Hand& hand)
{
	//hand.bestOption;

}

void findBest(std::vector<std::vector<Card*>>& options)
{
	for (int i = 0; i != options.size(); ++i) {
		//if (handValue(options[i]).value > bestValue.value) {
		//bestValue.value = handValue(options[i]).value;
		//bestValue.name = handValue(options[i]).name;
		//}
	}
}